---
- name: "Install basic apt packages"
  hosts: '{{ host }}'
  #remote_user: ubuntu
  tasks:
      - name: Apt install
        apt:
            pkg:
            - git
            - vim
            - ntp
            update_cache: yes
        become: yes
...

